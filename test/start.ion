#include "toinclude.ion";

func add(a) {
    return a + 1;
}


func main() {
    var a = (2 + 3) - ((1 + 1) + (1 + 1));
    a = 12;
    
    error(a);

    print("hallo!\n");
    print(a);
    error(a + 1);
    /* comment */
    const v = 3;
    const b = add(v) / add(2);
    print(b, v);
}

const b = "hi there!";
error(b);
main();